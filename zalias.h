#ifndef FT_PRINTF_H
# define FT_PRINTF_H

// Define:
#define _bashrc strcat(getenv("HOME"), "/.bashrc")
#define _zshrc strcat(getenv("HOME"), "/.zshrc")

#include <stdio.h>
#include <stdlib.h> 
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
#include <errno.h>

void	put_alias();

#endif
